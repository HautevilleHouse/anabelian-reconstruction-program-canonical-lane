# Identification Bridge

This note records the determining-class bridge for `anabelian reconstruction`. It fixes the endpoint representative, tracks `eps_coh`, and closes the final strict margin `M_ARP` in strict-coherence mode.
